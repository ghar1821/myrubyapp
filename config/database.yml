# Postgresql

development:
  adapter: postgresql
  database: myrubyapp_development
  pool: 5
  timeout: 5000
  username: myapp
  password: password
  host: localhost
  reconnect: false
  encoding: utf8

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  database: myrubyapp-test
  pool: 5
  timeout: 5000
  username: myapp
  password: password
  host: localhost
  reconnect: false
  encoding: utf8

production:
  adapter: postgresql
  database: myrubyapp-development
  pool: 5
  timeout: 5000
  username: myapp
  password: password
  reconnect: false
  encoding: utf8


